.gridItem.profile
  .start-year-text
    start year:&nbsp
  .start-year
    %select(data-ng-model="startYearString")
      %option(data-ng-repeat="year in years" value="{{year}}") {{year}}
  .total-units
    total units: {{units}}