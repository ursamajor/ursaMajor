.col-md-3
.col-md-9
  .col-md-12
    %h2= @rule.name.to_s
    %h3= @rule.description
    %table.table
      %thead
        %tr
          %th Courses Fulfilling
      .body
        - @courses.each do |course|
          %tr
            %th= link_to course.search_name, course_path(course)